use std::fs::File;
use std::io::{self, BufRead, BufReader};
use std::convert::From;
use std::iter::Iterator;

pub struct PGStream {
    input: String
}

impl PGtream {
    pub fn new() -> Self {
        GeoStream {
            input: String::from("")
        }
    }
}
